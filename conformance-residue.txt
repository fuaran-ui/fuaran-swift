# Conformance residue — the named set of things this surface is KNOWN not to answer.
#
# WHAT THIS IS. A committed list of the failures that stand on a clean tree against
# the current shared corpus. The gates read it and go red in BOTH directions:
#
#   * a failure NOT on this list is a REGRESSION      -> red, named;
#   * a line on this list that now PASSES is STALE    -> red, named, delete it.
#
# WHY IT EXISTS. This surface's gates were red BY DESIGN, and a run everyone
# expects to be red is a run nobody reads: a new regression lands inside an
# already-failing job with nothing to distinguish it from the standing failures.
# Naming the residue turns the expected red into a green with a stated cap, and
# gives a genuinely new failure somewhere to show up.
#
# WHAT IT IS NOT. It is not an exemption, and the distinction is the whole point.
# A DECLARED EXEMPTION (see `declaredExemptions` in RenderObligationTests.swift)
# says this surface STRUCTURALLY cannot answer a claim — it emits no attribute bag,
# no document, no network image loader — and is a permanent, reasoned answer. A
# residue entry says the surface does not model the slots the claim is about YET.
# That is unadopted work; it shrinks through adoption, never by reclassification.
# Turning a residue line into an exemption would make the gate green over a
# capability nobody has adopted, which is the one thing the mechanism prevents.
#
# HOW TO SHRINK IT. Adopt the vocabulary, register the checker or fix the decoder,
# delete the line. There is no way to make this file grow without it showing in a
# diff.

# ── Render obligations owed and unanswered (WIRE_FORMAT.md §13) ──────────────
#
# Both are reported by the gate as "unchecked with no checker registered", which
# is the honest answer — not checked is not passed, and silence is never an answer.
#
# THEIR REASON CHANGED at Phase 1499 even though their status did not. It was
# that this surface did not model the slots the claims are about; it models them
# now — `FileUploadSpec` carries `capture`, `destination`, `dropTarget`,
# `acceptPaste` and (Phase 1548) `maxBytes` / `maxFiles`, and `ModalSpec` carries
# `modality` — so what remains is exactly what these two lines say on their face:
# nobody has written the checker. That is still unadopted work and still not an
# exemption.
#
# THE SET DID NOT GROW AT PHASE 1548, and that is worth saying because the
# artefact did. `render-fidelity.json` gained a twentieth claim,
# `FileUpload/ceiling-recorded-never-enforced`, and it is a DECLARED EXEMPTION
# rather than a third line here: the claim is about a marker ATTRIBUTE on the
# static no-script tier, and this surface emits no attribute bag and no document,
# which is the structural test an exemption has to meet. The slots themselves are
# modelled and the floor follows the same obligation's reasoning — record THAT a
# ceiling was declared, never its value — pinned by UploadCeilingTests.swift.
#
# Verified against wire-format-fixtures/render-fidelity.json (20 declared
# obligations), the 15 registered `checkers` and the 3 `declaredExemptions`:
# 20 - 15 - 3 = these two. RENDER-PROJECTION.md states the same pair.
[obligation] FileUpload/picker-always-present
[obligation] Modal/aria-modal-only-when-blocking

# ── Reject vectors this decoder does not yet refuse ─────────────────────────
#
# EMPTY, AND MEASURED — Phase 1499, and STILL empty after Phase 1548 and Phase
# 1677. This
# section was previously empty because the landing machine had no Swift toolchain
# and the IDs could not be derived without running the decoder; it is empty now
# because there are none. Every reject vector in the shared corpus refuses with
# the pinned code at the pinned path, and `testEveryRejectFixtureIsRefused` is
# what says so: it fails on any vector this file does not name, so an empty
# section is a claim the suite re-checks on every run rather than an omission.
#
# THE CLAIM CARRIES ITS RUN, because "a Swift toolchain said so" is the only thing
# that can settle it and no estate machine has one. Most recently:
# `swift-ci` run 34664654776, job `swift-core`, where
# `-[FuaranUITests.CorpusTests testEveryRejectFixtureIsRefused]` passed in 0.075s
# against the corpus as it then stood. Citing a run rather than asserting the
# emptiness is the difference between a measured claim and a hopeful one; a
# machine reading this file offline cannot re-derive it, and should not pretend
# to.
#
# Phase 1548 added four to the corpus — the wrong shape and the non-positive
# value for each of `maxBytes` and `maxFiles` — and all four were accepted here
# until the ceilings were modelled, which is what put them briefly on this
# machine's run rather than on this list. The positivity floor is a DECODE RULE
# (§3.6.23), and each refusal is asserted beside its corrected twin in
# `UploadCeilingDecodeTests.swift`: a decoder that refused every ceiling outright
# would satisfy all four vectors and be wrong in the more expensive direction.
#
# Phase 1677 added six more, from three families that arrived together, and the
# section is still empty because each was CLOSED rather than listed: the two
# `I18n` argument vectors (an argument carrying a `$type` is a binding, so the
# binding's own refusals belong at the argument's path — the bag was previously
# checked only for an explicit null), the three §21.8 pipeline-expression
# vectors, and the §21.9 `Skeleton.rows` ceiling. The middle three are the
# instructive ones: the bound existed and was scoped to `Binding.Expr` alone, so
# the same 513-node expression was refused there and accepted by wrapping it in
# a `Transform` — a limit with a stated exclusion on the one surface an
# expression can be moved to is not a scope, it is a bypass. A residue line for
# those would have capped a hole rather than an unmodelled slot.
#
# The residue it recorded — the three form-field kinds, the widened clipboard
# payload, the four `FileUpload` members, `Modal.modality`, `SemanticStyle.direction`
# and the print-break controls — was adopted along with the rest of the vocabulary
# the corpus had grown around it. Two of the closures were places this decoder
# silently ACCEPTED a malformed document rather than failing to model a slot: the
# integer slot truncated a fraction, and the JSON number grammar rested on Swift's
# `Double(_:)`, which reads `03`, `3.`, `+1` and `inf` as perfectly ordinary
# numbers. Both are checked directly now.
